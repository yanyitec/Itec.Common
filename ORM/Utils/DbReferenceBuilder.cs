using Itec.ORM.DBs;
using Itec.ORM.Metas;
using System;
using System.Collections;
using System.Collections.Generic;
using System.Text;

namespace Itec.ORM.Utils
{
    public static class DbReferenceBuilder
    {
        //public static DbReference Build(DbMetaProperty prop) {
        //    if (prop.PropertyType.IsValueType) return null;
        //    if (prop.PropertyType == typeof(string)) return null;
        //    bool isMany = false;
        //    if (typeof(IEnumerable).IsAssignableFrom(prop.PropertyType)) isMany = true;
        //}
    }
}
